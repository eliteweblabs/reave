import type { ReavePlugin } from '../_shared/types';

/** Newsletter & email marketing — knowledge only (no agent tools yet). */
export const emailMarketingPlugin: ReavePlugin = {
  id: 'email-marketing',
  feature: 'email_marketing',
};
