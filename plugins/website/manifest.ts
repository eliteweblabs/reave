import type { ReavePlugin } from '../_shared/types';

/** Client-tier website pack — editor + stock photos. No hosting APIs. */
export const websitePlugin: ReavePlugin = {
  id: 'website',
  feature: 'website',
};
