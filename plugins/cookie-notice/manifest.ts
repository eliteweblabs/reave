import type { ReavePlugin } from '../_shared/types';

/** Public cookie notice — implied consent bar + /cookies policy. */
export const cookieNoticePlugin: ReavePlugin = {
  id: 'cookie-notice',
  feature: 'cookie_notice',
};
