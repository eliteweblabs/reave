node_modules
dist

# Environment variables
.env
.env.local
.env.*.local
# Snapshot from `railway variable list` (optional local sync)
.env.railway

# Runtime dashboard chat archives (markdown); commit manually if you want backups in git
src/knowledge/chats/*.md
src/knowledge/email-rules.json
src/knowledge/email-inbox.json

# Twilio credentials (already covered by .env, but being explicit)
# TWILIO_ACCOUNT_SID
# TWILIO_AUTH_TOKEN
