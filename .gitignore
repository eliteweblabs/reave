node_modules
dist

# Environment variables
.env
.env.local
.env.*.local
# Snapshot from `railway variable list` (optional local sync)
.env.railway

# Runtime dashboard data (not knowledge playbooks)
src/runtime/chats/*.md
src/runtime/email-rules.json
src/runtime/email-inbox.json
src/runtime/features.json
src/runtime/company-config.json
src/runtime/agent-model.json
src/runtime/jobs/*.md

# Twilio credentials (already covered by .env, but being explicit)
# TWILIO_ACCOUNT_SID
# TWILIO_AUTH_TOKEN
