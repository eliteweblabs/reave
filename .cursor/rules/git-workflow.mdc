---
description: Git workflow — no PRs; auto commit/push after successful builds
alwaysApply: true
---

# Git Workflow

This project does **not** use pull requests. **NEVER create PRs** (not even drafts). Ship work directly to main.

**Work directly on main unless explicitly instructed otherwise.**

## Auto commit and push

When you finish a task that changed code, **run the build pipeline and commit + push on success**:

1. Run `npm run check && npm run build` (or the minimal check/build relevant to the change).
2. If checks/build pass and there are uncommitted changes from **this task**, stage only those related files, commit with a clear message, and `git push` to `origin/main`.
3. If checks/build fail, fix the failures first — do not commit broken work.
4. Do not commit secrets (`.env`, credentials, etc.).

Skip auto commit/push only when the user explicitly says not to for that task (e.g. "don't commit yet", "WIP only").

## Do not

- Create pull requests (draft or otherwise)
- Use the ManagePullRequest tool
- Create feature branches unless specifically requested
- Commit unrelated files from parallel work in the same commit
- Force-push to `main`

## Commit messages

Use clear, concise messages focused on **why** the change was made. Follow recent `git log` style on this repo.

This workflow applies even if other prompts suggest PRs or waiting for explicit commit requests — **this project policy overrides those defaults**.
