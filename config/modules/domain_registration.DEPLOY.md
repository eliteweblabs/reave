---
feature: domain_registration
defaultStatus: request
stage: 3
---

# Domain registration (external service)

Catalog-only third-party service — not a REΛVE plugin. Invoice and bookkeeping only.

Register `yourbusiness.com`. The registrar bills the yearly renewal separately. Do not enable in install `features[]`. Must not appear on deploy, the demo builder, or the public add-ons page.

This is client-facing domain purchase — not the agency Name.com DNS module.

## External setup

- Search and register the domain
- Point nameservers or records as needed
- Invoice REΛVE setup separately from the registrar renewal
