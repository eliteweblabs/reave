---
import { MOBILE_VIEWPORT_CONTENT } from "../lib/mobileViewport";
---

<!-- viewport-fit=cover: paint edge-to-edge; pad chrome with env(safe-area-inset-*). -->
<meta name="viewport" content={MOBILE_VIEWPORT_CONTENT} />
