---
/**
 * Site atmosphere — CSS-tiled chevron pattern (viewport-fixed).
 * Mesh gradients live on `body` in Layout.
 */
import ReaveBgPattern from "./ReaveBgPattern.astro";
---

<ReaveBgPattern class="site-bg-pattern" />
