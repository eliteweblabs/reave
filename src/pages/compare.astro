---
import Layout from "../layouts/Layout.astro";
import ComparePageContent from "../components/compare/ComparePageContent.astro";
import { getCompanyConfig } from "../lib/companyConfig";

const company = await getCompanyConfig(Astro.request);
---

<Layout
  title="Compare"
  description={`How ${company.name} compares to a box-stock SaaS stack and bespoke custom development — industry waste stats, total cost of ownership, and the 90/10 Business OS model.`}
>
  <ComparePageContent />
</Layout>
