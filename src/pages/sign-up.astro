---
/**
 * Canonical sign-up UI is the same bottom sheet as /sign-in.
 * Clerk's "Sign up" footer lands here; send it to the multi-field form.
 */
const allowSignUp = import.meta.env.PUBLIC_CLERK_ALLOW_SIGN_UP !== "0";
return Astro.redirect(allowSignUp ? "/sign-in?mode=sign-up" : "/sign-in");
---
