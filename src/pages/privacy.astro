---
import Layout from "../layouts/Layout.astro";
import { getCompanyConfig } from "../lib/companyConfig";

const company = await getCompanyConfig(Astro.request);
const siteUrl = company.domain ? `https://${company.domain}` : Astro.url.origin;
const supportMail = company.supportEmail || (company.domain ? `support@${company.domain}` : "");
---

<Layout
  title="Privacy Policy"
  description={`Privacy Policy for ${company.name} — how we collect, use, and protect your information.`}
>
  <div class="page">
    <div class="doc">
      <div class="doc-header">
        <p class="doc-label">Legal</p>
        <h1>Privacy Policy</h1>
        <p class="doc-meta">{company.name} &mdash; Last updated June 16, 2025</p>
      </div>

      <div class="doc-body">
        <section>
          <h2>1. Overview</h2>
          <p>
            {company.legalName || company.name} ("we," "us," or "our") operates the platform available at
            {company.domain ? ` ${company.domain}` : ' this website'}. This Privacy Policy explains what information we collect, how we use it, and
            the choices you have. By using {company.name} you agree to the practices described here.
          </p>
        </section>

        <section>
          <h2>2. Information We Collect</h2>
          <h3>Information you provide</h3>
          <ul>
            <li>Name, email address, and phone number when you create an account or submit a form.</li>
            <li>Messages and requests you send through the platform (voice, text, or SMS).</li>
          </ul>
          <h3>Information collected automatically</h3>
          <ul>
            <li>Log data (IP address, browser type, pages visited, timestamps).</li>
            <li>Usage data generated when you interact with the platform.</li>
          </ul>
        </section>

        <section>
          <h2>3. SMS Communications</h2>
          <p>
            When you request information through the {company.name} platform, we may send you a transactional
            SMS message to the phone number you provide. These messages are sent only in direct
            response to your explicit request — we do not send marketing or promotional SMS.
          </p>
          <p>
            You can opt out of SMS at any time by replying <strong>STOP</strong> to any message. For
            help, reply <strong>HELP</strong>
            {supportMail ? (
              <> or contact us at <a href={`mailto:${supportMail}`}>{supportMail}</a></>
            ) : null}.
            Message and data rates may apply.
          </p>
        </section>

        <section>
          <h2>4. How We Use Your Information</h2>
          <ul>
            <li>To deliver the services and information you request.</li>
            <li>To operate, maintain, and improve the platform.</li>
            <li>To communicate with you about your account or service updates.</li>
            <li>To comply with legal obligations.</li>
          </ul>
          <p>We do not sell your personal information to third parties.</p>
        </section>

        <section>
          <h2>5. Data Sharing</h2>
          <p>
            We may share your information with trusted service providers who help us operate the
            platform (e.g., cloud hosting, communication infrastructure). These providers are
            contractually required to protect your data and may not use it for their own purposes.
          </p>
        </section>

        <section>
          <h2>6. Data Retention</h2>
          <p>
            We retain your information for as long as your account is active or as needed to provide
            services. You may request deletion of your data at any time by contacting us.
          </p>
        </section>

        <section>
          <h2>7. Security</h2>
          <p>
            We use industry-standard measures to protect your information. No method of transmission
            or storage is 100% secure, but we take reasonable steps to safeguard your data.
          </p>
        </section>

        <section>
          <h2>8. Your Rights</h2>
          <p>
            Depending on your location, you may have the right to access, correct, or delete your
            personal data. To exercise any of these rights, contact us
            {supportMail ? (
              <> at <a href={`mailto:${supportMail}`}>{supportMail}</a></>
            ) : ' through the platform'}.
          </p>
        </section>

        <section>
          <h2>9. Changes to This Policy</h2>
          <p>
            We may update this policy from time to time. When we do, we will revise the "Last
            updated" date at the top. Continued use of the platform after changes constitutes
            acceptance of the updated policy.
          </p>
        </section>

        <section>
          <h2>10. Contact</h2>
          <p>
            Questions?
            {supportMail ? (
              <> Reach us at <a href={`mailto:${supportMail}`}>{supportMail}</a></>
            ) : ' Contact us through the platform.'}
            {company.domain ? (
              <> or visit <a href={siteUrl}>{company.domain}</a>.</>
            ) : '.'}
          </p>
        </section>
      </div>
    </div>
  </div>
</Layout>

<style>
  * { box-sizing: border-box; }

  /* Dark background/text default now lives in Layout.astro. */

  .page {
    min-height: 100vh;
    padding: calc(64px + 48px) 24px 80px;
    display: flex;
    justify-content: center;
  }

  .doc {
    width: 100%;
    max-width: 720px;
  }

  .doc-header {
    margin-bottom: 40px;
    padding-bottom: 32px;
    border-bottom: 1px solid rgba(255,255,255,0.08);
  }

  .doc-label {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #a855f7;
    margin: 0 0 12px;
  }

  h1 {
    font-size: 32px;
    font-weight: 700;
    color: #fff;
    margin: 0 0 8px;
    letter-spacing: -0.02em;
  }

  .doc-meta {
    font-size: 14px;
    color: rgba(255,255,255,0.4);
    margin: 0;
  }

  .doc-body section {
    margin-bottom: 32px;
  }

  h2 {
    font-size: 18px;
    font-weight: 600;
    color: #fff;
    margin: 0 0 12px;
  }

  h3 {
    font-size: 15px;
    font-weight: 600;
    color: rgba(255,255,255,0.85);
    margin: 16px 0 8px;
  }

  p, li {
    font-size: 15px;
    line-height: 1.7;
    color: rgba(255,255,255,0.75);
    margin: 0 0 12px;
  }

  ul {
    padding-left: 20px;
    margin: 0 0 12px;
  }

  a {
    color: #a855f7;
    text-decoration: none;
  }

  a:hover { text-decoration: underline; }

  strong { color: rgba(255,255,255,0.9); }
</style>
