---
import Layout from "../layouts/Layout.astro";
import SiteAssistantWidget from "../components/SiteAssistantWidget.astro";
import HomeAddonsSection from "../components/home/HomeAddonsSection.astro";
import { getCompanyConfig } from "../lib/companyConfig";
import { hasFeature } from "../lib/features";
import { isSiteAssistantConfigured } from "../lib/siteAssistant";
import "../styles/marketing-sections.css";

const company = await getCompanyConfig(Astro.request);
const showSiteAssistant =
  hasFeature("portal_assistant") && isSiteAssistantConfigured();
---

<Layout
  title="Modules"
  description={`Optional modules for the ${company.name} platform — vertical add-ons for inventory, fleet, billing, documents, voice, and more.`}
>
  <div class="modules-page">
    <HomeAddonsSection />
  </div>

  {
    showSiteAssistant && (
      <SiteAssistantWidget
        brandTitle={company.name}
        supportPhone={company.supportPhone}
        supportEmail={company.supportEmail}
      />
    )
  }
</Layout>

<style>
  .modules-page {
    min-height: 100vh;
    padding: calc(
        var(--app-header-offset, calc(var(--app-header-h, 3.25rem) + env(safe-area-inset-top, 0px))) +
          40px
      )
      0 96px;
  }
</style>
