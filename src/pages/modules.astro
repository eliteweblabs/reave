---
/**
 * /modules — browse the add-on catalog (same UI as /demo-loader, no toggles).
 */
import Layout from "../layouts/Layout.astro";
import { getCompanyConfig } from "../lib/companyConfig";
import "../styles/demo-loader.css";

export const prerender = false;

const company = await getCompanyConfig(Astro.request);
---

<Layout
  title="Add-ons"
  description={`Optional add-ons for the ${company.name} platform — dealership, fleet, real estate, documents, voice, monitoring, and more.`}
>
  <div class="demo-loader-page">
    <header class="dl-hero">
      <p class="dl-kicker">Optional add-ons</p>
      <h1 class="dl-h1">Add-ons</h1>
      <p class="dl-lead">
        Vertical-specific add-ons that plug into the {company.name} platform,
        grouped as Social, E-commerce, and Web Development. Buy them from
        admin → Add-ons (or call). You cannot flip them on yourself. Every
        add-on shares the same admin, contacts, and AI agent.
      </p>
    </header>

    <div
      id="demo-loader-app"
      class="dl-app"
      data-toggles="false"
      aria-live="polite"
    >
      <p class="dl-loading">Loading add-ons…</p>
    </div>
  </div>

  <script is:inline src={`/demo-loader.js?v=${__PUBLIC_ASSET_VERSION__}`}></script>
</Layout>
