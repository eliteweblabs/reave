---
/**
 * Public inner-page layout — same chrome as Layout.
 * Marketing help chat is intentionally not mounted; portal keeps its own widget.
 */
import Layout from "./Layout.astro";
import type { Props as LayoutProps } from "./Layout.astro";

export type Props = LayoutProps;

const props = Astro.props;
---

<Layout {...props}>
  <slot />
</Layout>
